/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

//

module.exports = {
  images: {
    domains: [
      "placeimg.com",
      "localhost",
      "hollow-glowing-gladiolus.glitch.me",
    ],
  },
};
